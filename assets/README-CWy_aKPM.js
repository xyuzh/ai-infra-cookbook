const t="# Frontier Infra Stack explorer\n\nThe interactive map and cookbook for [anyscale/examples](https://github.com/anyscale/ai-infra-cookbook): a pan/zoom pipeline chart of every example (curate → train → post-train → serve), guided journeys, a `⌘K` command palette, and a docs-style page per example — sidebar navigation, the full README with copyable code blocks, an \"On this page\" rail, and upstream/downstream links.\n\nBuilt with [Vite](https://vite.dev), [React](https://react.dev), and [React Flow](https://reactflow.dev). No backend — the build is fully static.\n\n## Develop\n\n```bash\nnpm install\nnpm run dev        # http://localhost:5173\n```\n\n## Validate\n\n```bash\nnpm run check\n```\n\nTypechecks and runs [`scripts/validate-catalog.mts`](scripts/validate-catalog.mts), which keeps [`src/data/catalog.ts`](src/data/catalog.ts) — the single source of truth for the map — consistent with the example directories and READMEs: directory ↔ catalog bijection, edge/journey integrity, dead `blob/main` links, verbatim run commands, and matching \"Position in the stack\" sections.\n\n## Build & deploy\n\n```bash\nnpm run build      # emits dist/ with relative asset paths\nnpm run preview\n```\n\n`vite.config.ts` sets `base: './'` and the app uses hash routing, so the build works at any path — the live deployment at [xyuzh.github.io/ai-infra-cookbook](https://xyuzh.github.io/ai-infra-cookbook/), a GitHub Pages project site, a subdirectory, or a local preview.\n\nTo publish to the live site, copy the build into `public/ai-infra-cookbook/` in the [xyuzh/xyuzh.github.io](https://github.com/xyuzh/xyuzh.github.io) checkout and push — its CI rebuilds and deploys Pages.\n\nThe repository also ships its own GitHub Pages workflow at [`.github/workflows/deploy-site.yml`](../.github/workflows/deploy-site.yml). It stays inert until a maintainer enables Pages for the repo (**Settings → Pages → Source: GitHub Actions**); after that, pushes to `main` that touch `site/` deploy automatically.\n\n## How content flows\n\n- **`src/data/catalog.ts`** — every example's stage, summary, stack citations, run commands, key files, edges, and journey membership. Edit this to change the map and page chrome.\n- **Example pages** — each page's body is the example's `README.md`, imported from the repository at build time ([`src/lib/content.ts`](src/lib/content.ts)), so pages always match the checkout exactly and work offline. The page header (title, summary, meta) and the position cards come from the catalog; the README's own title and Position section are stripped to avoid duplication.\n- **Layout** — [`src/lib/graph-layout.ts`](src/lib/graph-layout.ts) computes static stage-column positions; no layout library involved.\n";export{t as default};
